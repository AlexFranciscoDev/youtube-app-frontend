/**
 * Taller guiado para practicar tests del formulario de Login.
 *
 * Importante:
 * - Este archivo NO termina en `.test.tsx` a propósito.
 * - Puedes usarlo como borrador sin que Vitest lo ejecute todavía.
 * - Cuando quieras probarlo, renómbralo o copia el contenido a un archivo `.test.tsx`.
 *
 * Reto 1:
 * - Renderiza `Login`.
 * - Busca el input de email por su label.
 * - Busca el botón "Sign in".
 *
 * Reto 2:
 * - Escribe un email inválido.
 * - Provoca blur o submit.
 * - Comprueba que aparece el mensaje de error.
 *
 * Reto 3:
 * - Piensa qué dependencias externas tiene `Login`.
 * - Decide cuáles deberías mockear para testear el flujo feliz.
 *
 * Pista Vitest/Jest:
 * - En Vitest usarás `vi.fn()`.
 * - En Jest sería `jest.fn()`.
 */
export {}
