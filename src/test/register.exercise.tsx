/**
 * Taller guiado para practicar tests del formulario de Register.
 *
 * Importante:
 * - Este archivo NO termina en `.test.tsx` para que puedas trabajar sin romper la suite.
 *
 * Reto 1:
 * - Renderiza `Register`.
 * - Localiza username, email, password, confirm password e image.
 *
 * Reto 2:
 * - Envía el formulario vacío.
 * - Comprueba qué mensajes de error deberían aparecer.
 *
 * Reto 3:
 * - Sube un archivo al input de imagen.
 * - Comprueba que aparece la preview.
 *
 * Reto 4:
 * - Piensa cómo mockearías `fetch` para un registro correcto.
 *
 * Pista Vitest/Jest:
 * - `vi.spyOn(...)` en Vitest equivale a `jest.spyOn(...)` en Jest.
 */
export {}
